{
  "name": "pdf-to-json",
  "version": "1.0.0",
  "description": "This is an approach to extract a JSON representation of the text in a PDF",
  "main": "dist/index.js",
  "bin": {
    "pdf-to-json": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lgriffin",
  "license": "MIT",
  "dependencies": {
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
